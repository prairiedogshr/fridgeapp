import { ADD_EXPENSE } from './actionTypes.js';

export const addExpense = () => {
  return (dispatch, getState) => {
    console.log(getState());
    // axios.post(`/api/expenses`, {

    // })
  }
}